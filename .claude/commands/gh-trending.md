Fetch the top trending GitHub repositories from the past week (English-language).

Run `./scripts/getGHTrendingForPastWeek.sh` and return the JSON result. Each entry has `{name, url, starsThisWeek}`.
